apply plugin: 'java'

version = '0.1.0'

repositories {
    jcenter()
}

dependencies {
    testCompile 'junit:junit:4.11'
}

task sourcesZip(type: Zip) {
    classifier = 'src'
    from 'src/main/java'
}
